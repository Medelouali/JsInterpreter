#include<vector>
#include"lexer.h"

#define MAXLEN 3

//There are two cases binary operators or unary operators that's it;
std::vector<std::string> lexer(std::string& base){
    std::vector<std::string> vect;
    std::string op;
    long unsigned int n=base.length();
    
    return vect;
};

//we will continue tomorrow